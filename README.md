# Weather Dashboard App

یک داشبورد حرفه‌ای هواشناسی ساخته شده با **React + Vite + TypeScript** که اطلاعات آب‌وهوا را از API رایگان **Open‑Meteo** دریافت می‌کند. رابط کاربری با **Material UI** طراحی شده و از **i18n** برای چندزبانه بودن (فارسی / انگلیسی) پشتیبانی می‌کند.

---

## 🚀 ویژگی‌ها

* دریافت **وضعیت فعلی** آب‌وهوا
* نمایش **پیش‌بینی ۱۴ روزه**
* نمایش **میانگین ماهانه** در قالب نمودار (Recharts)
* پشتیبانی از **تغییر شهر**
* طراحی **کاملاً رسپانسیو**
* پشتیبانی از **Dark / Light Theme**
* تاریخ **جلالی / میلادی** با استفاده از `jalali‑moment`
* مدیریت داده‌ها با **Context API**
* ساختار پوشه‌ای تمیز و قابل‌توسعه

---

## 📂 ساختار پروژه

```
src/
│── services/
│   └── weather.ts        → درخواست‌های axios
│── context/
│   └── themeContext.tsx
│── components/
│   ├── weatherCard.tsx
│   ├── Forecast.tsx
    ├── ForecastCard.tsx
│   ├── TemperatureChart.tsx
│   └── nav.tsx
│── locales/
│── images/
│── font/
│── pages/
│── App.tsx
│── main.tsx
```

---

## 🛠️ تکنولوژی‌های استفاده‌شده

* **React 18**
* **Vite**
* **TypeScript**
* **Axios**
* **Material UI (MUI)**
* **Recharts**
* **i18next**
* **jalali-moment**
* **Context API**
* **JS Cookie**

---

## 📦 نصب و اجرا

### 1. کلون کردن پروژه

```bash
git clone https://github.com/yourusername/weather-app.git
cd weather-app
```

### 2. نصب پکیج‌ها

```bash
npm install
```

### 3. اجرای پروژه

```bash
npm run dev
```

پروژه روی این آدرس بالا میاد:

```
http://localhost:5173
```

---

## 🌤️ API مورد استفاده

پروژه از **Open‑Meteo** استفاده می‌کند چون:

* رایگان است ✅
* کلید API نمی‌خواهد ✅
* CORS‑Free ✅
* داده‌های کامل روزانه و پیش‌بینی ارائه می‌دهد ✅

نمونه URL:

```
https://api.open-meteo.com/v1/forecast?
latitude=35.6892&longitude=51.3890&current_weather=true
&daily=temperature_2m_max,temperature_2m_min,weathercode
&forecast_days=14&timezone=auto
```

---

## 🧮 قابلیت چندزبانه (i18n)

* پشتیبانی کامل از فارسی/انگلیسی
* تشخیص جهت نوشتار RTL/LTR
* تاریخ جلالی / میلادی


---

## 👨🏼‍💻 توسعه‌دهنده

**Sepehr**


---

## 📄 لایسنس

MIT License
